[server]
headless = true
enableXsrfProtection = true

[browser]
gatherUsageStats = false

# Pure Air brand palette (pulled from pure-comfort.com stylesheet).
[theme]
base = "light"
primaryColor = "#0066EE"             # Pure Air blue — buttons, sliders, focus rings
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F7F7F7" # sidebar + secondary panels
textColor = "#1A1A1A"
font = "sans serif"
